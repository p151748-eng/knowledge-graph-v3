# KG-Agent System 项目介绍

## 项目概述

KG-Agent System 是一个面向个人学习、知识管理与论文阅读场景的智能知识库系统。系统结合 RAG、知识图谱、多 Agent 工作流、对话记忆与证据校验机制，支持文档导入、知识沉淀、混合检索、智能问答、论文分析和复杂研究任务规划。

系统通过上下文感知路由器对用户问题进行意图识别与轻量级任务规划，根据问题类型动态选择 FastPath、KG Pipeline、Self-RAG、Paper Assistant 或 Research Workflow 等执行路径，在响应效率、上下文连续性和答案可信度之间取得平衡。

---

## 问题类型设计

系统将用户问题划分为全局路由问题类型、论文助手内部任务类型和复杂研究工作流子任务类型三层。

### 一、全局问题类型 / 路由类型

| 中文问题类型 | 英文名称 | 对应路径 | 说明 |
|---|---|---|---|
| 简单事实问答 | Fact QA / Simple QA | `fast` | 定义、概念解释、简单知识问答，例如“什么是 RAG？” |
| 通用知识问答 | General QA | `fast` | 不一定依赖复杂推理的普通知识类问题 |
| 复杂分析问题 | Complex Analysis | `pipeline` | 需要分析、比较、归纳、关系推理的问题 |
| 证据验证 / 时效性问题 | Evidence Verification / Freshness QA | `self_rag` | 需要验证结论可靠性、查证最新信息、判断是否有依据的问题 |
| 单篇论文理解问题 | Single-Paper Understanding | `paper_assistant` | 针对某一篇论文的摘要、方法、实验、结论等问题 |
| 研究规划 / 方案设计问题 | Research Planning / Research Design | `research` | 研究综述、技术路线、方案设计、研究空白、优化规划类问题 |
| 上下文追问 | Contextual Follow-up | 继承上一轮路径 | “这个呢？”“继续刚才”“为什么？”等依赖历史上下文的问题 |
| 多对象比较问题 | Multi-object Comparison | `pipeline` / `research` | 多个概念、方法、论文、方案之间的对比分析 |
| 闲聊 / 非知识库对话 | Casual Chat / Chit-chat | 通常 `fast` | 普通对话、闲聊、非严肃检索型问题 |

---

### 二、论文助手内部任务类型

| 中文任务类型 | 英文名称 | 内部类型 |
|---|---|---|
| 论文概览 | Paper Overview | `overview` |
| 普通论文问答 | Paper QA | `paper_qa` |
| 方法解释 | Method Explanation | `method` |
| 实验分析 | Experiment Analysis | `experiment` |
| 方法对比 | Method Comparison | `compare` |
| 相关工作 / 文献综述 | Literature Review / Related Work Review | `literature_review` |
| 写作大纲 / 开题提纲 | Writing Outline / Proposal Outline | `outline` |

---

### 三、复杂研究工作流子任务类型

| 中文子任务类型 | 英文名称 | 内部类型 |
|---|---|---|
| 检索论文 | Retrieve Papers | `retrieve_papers` |
| 检索知识图谱上下文 | Retrieve KG Context | `retrieve_kg_context` |
| 总结证据 | Summarize Evidence | `summarize_evidence` |
| 总结方法 | Summarize Method | `summarize_method` |
| 比较方法 | Compare Methods | `compare_methods` |
| 提取实验信息 | Extract Experiments | `extract_experiments` |
| 发现研究空白 | Find Research Gap | `find_research_gap` |
| 生成写作大纲 | Generate Outline | `generate_outline` |
| 生成研究方案 | Generate Proposal | `generate_proposal` |
| 校验关键主张 | Verify Claims | `verify_claims` |

---

## 核心机制概括

### 轻量级任务规划与工作流编排

系统在用户输入后先进行意图识别、复杂度评估和最小必要规划，再动态路由至不同执行路径。简单事实问答和闲聊走 FastPath，复杂分析走 KG Pipeline，需要证据验证的问题走 Self-RAG，论文理解走 Paper Assistant，研究规划和方案设计走 Research Workflow。

### 四层记忆压缩与意图聚焦机制

系统融合近期对话、长期摘要、结构化任务状态和向量化记忆召回，构建 Memory Context；同时通过 active_focus 跟踪当前关注主题，识别“继续刚才”“换一个”“回到前面”“对比一下”等语义信号，缓解长对话中的上下文遗忘、指代混乱和语义漂移。

### 分层证据校验机制

系统在生成前通过 EvidenceGrader 判断检索证据是否足以回答问题，并决定是否需要查询改写、调整检索策略、联网验证或拒答；在生成后通过 AnswerVerifier 和 CitationVerifier 校验答案是否被证据支撑，并输出主张级证据覆盖情况。

### 论文助手任务规划

Paper Assistant 通过 PaperTaskRouter 识别论文概览、方法解释、实验分析、方法对比、文献综述和写作大纲等任务类型，并为不同任务生成 chunk 偏好、检索策略和回答结构，实现面向论文阅读场景的结构化问答。

---

## 路由机制与路径节点设计

系统采用“上下文感知路由 + 最小必要规划 + 分路径工作流”的设计。用户输入进入 ChatService 后，系统先构建近期历史和 Memory Context，再由 ContextAwareRouter 输出 RouteDecision，最后由 WorkflowRegistry 调度对应路径。

整体流程如下：

```text
用户问题
  ↓
加载近期对话与 Memory Context
  ↓
ContextAwareRouter 进行意图识别、复杂度评估和上下文判断
  ↓
输出 RouteDecision(path / intent / topic_changed / context_dependent / confidence / reason)
  ↓
WorkflowRunner 调度对应 Workflow
  ↓
返回答案、证据来源、置信度、校验结果和执行轨迹
```

### 一、路由判断逻辑

| 判断条件 | 路由结果 | 说明 |
|---|---|---|
| 用户显式指定 `force_path` | 指定路径 | 直接使用用户指定的 `fast`、`pipeline`、`self_rag`、`paper_assistant` 或 `research` |
| 当前问题是上下文追问 | 继承上一轮路径 | 例如“继续”“这个呢”“为什么”，系统复用上一轮 path，避免语义断裂 |
| 包含“论文、摘要、方法、实验、引用”等关键词 | `paper_assistant` | 单篇论文理解、方法解释、实验分析等任务 |
| 包含“最新、当前、验证、依据、是否真实”等关键词 | `self_rag` | 需要时效性、证据验证或可靠性判断的问题 |
| 包含“研究综述、方案、规划、技术路线、架构设计”等关键词 | `research` | 需要复杂规划、研究分析或方案生成的问题 |
| 包含“分析、对比、比较、关系、区别、整合”等关键词 | `pipeline` | 普通知识库中的复杂分析或关系型问题 |
| 问题较短或属于定义类问题 | `fast` | 简单事实问答、闲聊或低复杂度知识问答 |

### 二、各路径对应节点

| 路径 | 中文名称 | 主要节点 / 阶段 | 是否强依赖大模型 | 适用问题 |
|---|---|---|---|---|
| `fast` | 快速问答路径 | 混合检索节点 → 直接生成节点 → 轻量引用校验节点 | 生成节点依赖 LLM；检索部分可部分依赖 Embedding | 简单事实问答、通用知识问答、闲聊 |
| `pipeline` | KG 多 Agent 分析路径 | KG 检索节点 → 术语解释节点 → 答案整合节点 | 解释和整合依赖 LLM | 复杂知识分析、概念关系、对比归纳 |
| `self_rag` | 自校验 RAG 路径 | 本地检索节点 → 证据评估节点 → 检索修正节点 → 联网验证节点 → 答案生成节点 → 答案校验节点 | 证据评估、生成、答案校验依赖 LLM | 证据验证、时效性问题、可靠性判断 |
| `paper_assistant` | 论文助手路径 | 论文任务规划节点 → 论文证据检索节点 → 证据评估节点 → 检索修正节点 → 专家生成节点 → 引用校验节点 | 任务生成、证据评估、专家生成依赖 LLM | 单篇论文概览、方法解释、实验分析、论文对比 |
| `research` | 研究规划工作流路径 | 问题解析与规划节点 → 多源证据召回节点 → 证据驱动生成节点 → 主张级证据校验节点 → 结果聚合输出节点 | 规划、生成、校验依赖 LLM；结果聚合不依赖 | 研究综述、方案设计、技术路线、研究空白分析 |

---

## 各路径执行流程说明

### 1. FastPath：快速问答路径

FastPath 面向简单事实问答、普通知识问答和闲聊类问题，目标是以最低成本快速响应。

```text
用户问题
  ↓
混合检索节点
  ↓
直接生成节点
  ↓
轻量引用校验节点
  ↓
返回答案
```

节点说明：

| 节点 | 作用 |
|---|---|
| 混合检索节点 | 调用 HybridSearchTool，从知识库、文档 chunk 或图谱中召回少量相关证据 |
| 直接生成节点 | 将用户问题、Memory Context 和检索上下文拼接后交给 LLM 生成回答 |
| 轻量引用校验节点 | 使用 CitationVerifier 对答案进行主张级证据匹配，并将结果写入 metadata，不强制阻断 |

特点：

- 路径最短，延迟最低；
- 不做复杂任务拆解；
- 不强制联网验证；
- 适合“是什么”“介绍一下”“简单解释”等问题；
- 对闲聊类问题也可以直接响应，避免过度规划。

---

### 2. KG Pipeline：多 Agent 知识分析路径

KG Pipeline 面向复杂知识问答，适合分析、比较、关系推理和概念整合。

```text
用户问题
  ↓
KG 检索节点
  ↓
术语解释节点
  ↓
答案整合节点
  ↓
返回答案
```

节点说明：

| 节点 | 作用 |
|---|---|
| KG 检索节点 | 从知识图谱和文档中检索实体、关系和上下文 |
| 术语解释节点 | 将检索到的专业概念转化为更容易理解的解释 |
| 答案整合节点 | 综合实体、关系、解释和上下文，生成结构化回答 |

特点：

- 比 FastPath 更适合多概念、多关系问题；
- 通过“检索—解释—整合”拆分复杂问答；
- 不默认联网；
- 适合“比较 A 和 B”“A 与 B 有什么关系”“为什么会这样”等问题。

---

### 3. Self-RAG：自校验 RAG 路径

Self-RAG 面向需要可靠性判断、证据验证或时效性的用户问题，是最强调可信度的一条路径。

```text
用户问题
  ↓
本地检索节点
  ↓
证据评估节点
  ↓
证据不足？
  ├─ 是：检索修正节点 / 联网验证节点
  └─ 否：进入答案生成
  ↓
答案生成节点
  ↓
答案校验节点
  ↓
必要时拒答或返回答案
```

节点说明：

| 节点 | 作用 |
|---|---|
| 本地检索节点 | 先从本地知识库执行混合检索 |
| 证据评估节点 | EvidenceGrader 判断证据相关性、支撑度、覆盖度和是否需要 freshness |
| 检索修正节点 | 当证据不足时，改写查询或调整检索策略后再次检索 |
| 联网验证节点 | 当问题需要最新信息或本地证据不足时，调用 WebSearchVerifier 获取外部证据 |
| 答案生成节点 | 基于本地证据和联网证据生成回答 |
| 答案校验节点 | AnswerVerifier 判断答案是否被证据支撑，必要时拒答 |

特点：

- 生成前校验证据是否足够；
- 生成后校验答案是否被证据支撑；
- 可以触发查询改写、本地重试和联网验证；
- 是强校验路径，证据不足时可以拒答；
- 适合“这个结论可靠吗”“有没有证据”“现在最新情况是什么”等问题。

---

### 4. Paper Assistant：论文助手路径

Paper Assistant 面向单篇论文理解和论文阅读辅助，核心是先识别论文任务类型，再选择对应的检索策略和专家回答方式。

```text
用户问题
  ↓
论文任务规划节点
  ↓
论文证据检索节点
  ↓
证据评估节点
  ↓
证据不足？
  ├─ 是：检索修正节点
  └─ 否：进入专家生成
  ↓
专家生成节点
  ↓
引用校验节点
  ↓
返回论文分析结果
```

节点说明：

| 节点 | 作用 |
|---|---|
| 论文任务规划节点 | PaperTaskRouter 判断任务类型，如论文概览、方法解释、实验分析、相关工作、方法对比、写作大纲 |
| 论文证据检索节点 | 根据任务类型设置 chunk 偏好和检索参数，例如 method、experiment、table、reference 等 |
| 证据评估节点 | 判断检索结果是否足够支撑论文问答 |
| 检索修正节点 | 证据不足时重写查询、调整检索策略并二次检索 |
| 专家生成节点 | 根据任务类型调用对应专家 Agent，生成结构化论文回答 |
| 引用校验节点 | 对输出结果进行主张级证据匹配，记录 citation_verification |

特点：

- 不是通用问答，而是论文任务型问答；
- 不同论文任务对应不同证据偏好；
- 方法问题优先 method/equation/code；
- 实验问题优先 experiment/table；
- 综述问题优先 related_work/reference；
- 支持多轮论文追问时结合 Memory Context 和 active_focus 维持当前论文焦点。

---

### 5. Research Workflow：研究规划工作流路径

Research Workflow 面向研究综述、方案设计、技术路线、研究空白分析等复杂任务，是规划粒度最高的一条路径。

```text
用户问题
  ↓
问题解析与规划节点
  ↓
多源证据召回节点
  ↓
证据驱动生成节点
  ↓
主张级证据校验节点
  ↓
结果聚合输出节点
```

节点说明：

| 节点 | 作用 | 是否依赖 LLM |
|---|---|---|
| 问题解析与规划节点 | 将用户目标拆解为受控子任务，并校验任务类型、依赖关系和预算约束 | 依赖 LLM，但有 fallback 兜底 |
| 多源证据召回节点 | 根据规划结果调用 HybridSearchTool 检索论文、文档、图谱和上下文证据 | 部分依赖，语义检索可能依赖 Embedding |
| 证据驱动生成节点 | 基于执行计划和证据生成结构化研究回答 | 强依赖 LLM |
| 主张级证据校验节点 | 抽取回答中的关键主张，判断是否被证据支撑 | 当前 CitationVerifier 为规则式匹配，不强依赖 LLM |
| 结果聚合输出节点 | 清理临时状态，输出答案、证据、校验结果、置信度和 trace | 不依赖 LLM |

特点：

- 通过受控任务集合限制规划范围；
- 通过 PlanningBudget 限制子任务数、检索次数、LLM 调用次数和运行时间；
- 必须包含 verify_claims 子任务；
- 使用状态图编排 planner → retrieval → synthesis → citation_verifier → finalize；
- 每个节点执行后都会写入 trace，便于追踪复杂任务的执行过程。

---

## 路径差异总结

| 维度 | `fast` | `pipeline` | `self_rag` | `paper_assistant` | `research` |
|---|---|---|---|---|---|
| 主要目标 | 快速回答 | 复杂知识分析 | 高可信问答 | 论文理解 | 研究规划与方案生成 |
| 是否任务拆解 | 否 | 弱拆解 | 中等拆解 | 任务类型规划 | 强任务规划 |
| 是否使用记忆 | 是 | 是 | 是 | 是 | 是 |
| 是否使用混合检索 | 是 | 是 | 是 | 是 | 是 |
| 是否使用知识图谱 | 可用 | 重点使用 | 可用 | 按任务决定 | 可用 |
| 是否联网 | 否 | 否 | 必要时联网 | 默认不联网 | 可扩展 |
| 是否证据评估 | 轻量引用校验 | 不作为主流程 | 强证据评估 | 证据评估 + 引用校验 | 主张级校验 |
| 是否可能拒答 | 一般不拒答 | 一般不拒答 | 证据不足可拒答 | 谨慎提示证据不足 | 输出校验结果 |
| 适合场景 | 定义、闲聊、简单问答 | 对比、关系、分析 | 查证、可靠性、最新信息 | 单篇论文阅读 | 综述、方案、技术路线 |

